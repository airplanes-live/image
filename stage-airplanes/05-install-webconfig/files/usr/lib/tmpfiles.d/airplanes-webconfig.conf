# systemd-tmpfiles spec — creates /run/airplanes at boot. apply-config flocks
# /run/airplanes/feed-env.lock when rewriting feed.env; first-run.service
# uses the same lock when merging the boot-config.txt template into
# feed.env on first boot.
d /run/airplanes 0755 root root -
