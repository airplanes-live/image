[Unit]
# Defense-in-depth: the chroot step in 01-run-chroot.sh masks
# getty@tty1.service. If the mask is later cleared (operator unmask, or a
# `systemctl preset-all` run), this Conflicts= still prevents the two units
# from racing for /dev/tty1.
Conflicts=airplanes-dashboard.service
