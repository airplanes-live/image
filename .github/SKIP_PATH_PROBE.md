Temporary probe file — verifies that PRs touching no image-build inputs
get skipped-but-reported build checks. This PR is closed unmerged once
the merge box is observed.

Re-probe after the static-name + ruleset fixes.
